{
    "name": "htmlc-tools",
    "description": "lang server/client for html chunk loader in vscode",
    "author": "abschill",
	"publisher": "abschill",
    "license": "MIT",
    "version": "1.0.1",
    "keywords": [
		"html-chunk-loader"
	],
    "scripts": {
      "vscode:prepublish": "npm run compile",
      "compile": "tsc -b",
      "watch": "tsc -b -w",
      "lint": "eslint ./client/src ./server/src --ext .ts,.tsx",
      "postinstall": "cd client && npm install && cd ../server && npm install && cd ..",
      "test": "sh ./scripts/e2e.sh"
    },
    "main": "./client/out/extension",
	"engines": {
		"vscode": "^1.66.0"
	},
	"repository":{
		"type": "git",
		"url": "https://github.com/abschill/hcl-lsp-vscode"
	},
	"activationEvents": [
		"onLanguage:plaintext",
		"onlanguage:html"
	],
    "contributes": {
      "configuration": {
        "type": "object",
        "title": "Example configuration",
        "properties": {
          "languageServerExample.maxNumberOfProblems": {
            "scope": "resource",
            "type": "number",
            "default": 100,
            "description": "Controls the maximum number of problems produced by the server."
          },
		  "hclLanguageServer.featureSupportVersion": {
			"scope": "application",
			"type": "array",
			"default": [0,6,14],
			"description": "Language Server Feature Version"
		  },
          "languageServerExample.trace.server": {
            "scope": "window",
            "type": "string",
            "enum": [
              "off",
              "messages",
              "verbose"
            ],
            "default": "off",
            "description": "Traces the communication between VS Code and the language server."
          }
        }
      }
	},
	"devDependencies": {
		"@types/mocha": "^9.1.1",
		"@types/node": "^17.0.26",
		"@typescript-eslint/eslint-plugin": "^5.20.0",
		"@typescript-eslint/parser": "^5.20.0",
		"eslint": "^8.14.0",
		"mocha": "^9.2.2",
		"typescript": "^4.6.3"
	}
}
